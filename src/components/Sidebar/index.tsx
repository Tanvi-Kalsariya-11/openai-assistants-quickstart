import React from "react";
import { ChatBubbleIcon, MessagesIcon, PersonIcon } from "../Icons";
import { Button } from "../Button";
import SidebarNavigations from "../SidebarNavigations";

const Sidebar = () => {
  
  return (
    <div className="flex">
      <SidebarNavigations />
    </div>
  );
};

export default Sidebar;
